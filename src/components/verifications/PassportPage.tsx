import React from 'react';
import { Text, Center } from 'native-base';

export default function PassportPage() {
    return (
        <Center flex={1}>
            <Text>HJE:</Text>
        </Center>
    );
}